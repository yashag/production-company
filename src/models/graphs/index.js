const { createAverageScoreGraph } = require('./average-score-graph/average-score-graph.model');
const { createMoviesPerMonthGraph } = require('./movies-per-month-graph/movies-per-month-graph.model');

module.exports = {
    createAverageScoreGraph,
    createMoviesPerMonthGraph
}